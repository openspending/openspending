language: python
python:
  - "2.7"
services:
  - rabbitmq
  - postgresql
before_install:
  - git clone git://github.com/openspending/openspending-installer.git
install:
  - openspending-installer/vagrant/install.sh
script: "nosetests"
